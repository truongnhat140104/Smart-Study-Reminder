//
//  ScanImageCard.swift
//  Smart Study Reminder
//

import SwiftUI

/// Giữ file này để những chỗ cũ đang gọi `ScanImageCard()` không bị lỗi.
/// Flow scan thật đã nằm trong `ScanTimetableCard`.
struct ScanImageCard: View {
    var body: some View {
        ScanTimetableCard()
    }
}
